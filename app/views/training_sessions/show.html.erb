<%= render("training_sessions/show/top_actions", training_session: @training_session) %>
<%= render("training_sessions/show/infos", training_session: @training_session) %>
<%= render("training_sessions/show/editors_list", training_session: @training_session) %>
<%= render("training_sessions/show/roles_list", training_session: @training_session) %>
