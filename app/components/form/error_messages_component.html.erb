<% if @object.errors.any? %>
  <% @object.errors.full_messages.each do |message| %>
    <div
      class="bg-red-800 text-red-100 border border-red-900 rounded-2xl py-2 px-3 text-sm mb-2">
      <div>
        <%= message %>
      </div>
    </div>
  <% end %>
<% end %>
